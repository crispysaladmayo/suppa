# Suppa M2 — Project Spec

Generated by harness-run · April 2026

## Summary

Full revamp of the Suppa child nutrition webapp, internal tools, and backend.

## Structure

```
Suppa/
  docs/
    PRD.md              ← Product requirements (personas, features P0–P2, metrics)
    DESIGN_BRIEF.md     ← Brand tokens, typography, components, copy voice, a11y
  design/               ← Checkpoint 3: standalone HTML design review files
    design-tokens.css
    design-landing.html
    design-today.html
    design-log.html
    design-recipes.html
    design-settings.html
    design-internal.html
  app/                  ← Consumer webapp (all screens, vanilla JS, split CSS)
    css/tokens.css      ← CSS custom properties
    css/components.css  ← Shared component styles
    js/tokens.js        ← Shared constants (API_BASE, macros, food groups)
    js/profile.js       ← localStorage helpers
    js/api.js           ← Fetch wrappers
    js/ui.js            ← Toast, sheet, focus trap, tabs
    js/app.js           ← DOMContentLoaded orchestrator
    index.html          ← Landing
    signup.html         ← Sign up
    login.html          ← Login
    onboarding.html     ← 5-step wizard
    today.html          ← Today dashboard
    log-add.html        ← Log a meal
    log-week.html       ← Weekly history
    fridge.html         ← Fridge ingredient entry
    fridge-results.html ← Fridge results (guarded)
    recipes.html        ← Recipe library (tablist a11y)
    recipe-detail.html  ← Recipe detail + share sheet
    add-recipe.html     ← Add custom recipe
    growth.html         ← Growth tracking
    edukasi.html        ← Education hub
    edukasi-article.html← Article + quiz
    meal-prep.html      ← Weekly meal prep planner
    settings.html       ← Settings (all rows wired)
    forgot-password.html← Forgot/reset flow
    privacy.html        ← Privacy policy (stub)
    terms.html          ← Terms of service (stub)
    assets/suppa-logo.svg
  api/                  ← Express.js backend
    server.js           ← Main server (router, validation, logging, CRUD)
    db.json             ← Flat-file database (auto-seeded)
    package.json
    node_modules/
  internal/             ← CRUD console (served by API at localhost:8787/)
    index.html          ← Full CRUD UI with search, inline edit, delete confirm
  SPEC.md               ← This file
  GENERATOR_NOTES.md    ← Build notes
  EVALUATION.md         ← Evaluator findings
```

## Run the API

```bash
cd Suppa/api
npm start
# → http://localhost:8787/        (Internal CRUD console)
# → http://localhost:8787/api/    (REST API)
```

Open `app/index.html` in a browser for the consumer webapp.
