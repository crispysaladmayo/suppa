# Dependencies
api/node_modules/

# Runtime database (seeded automatically on first start)
api/db.json

# Environment secrets
.env
api/.env

# macOS
.DS_Store
**/.DS_Store

# Editor (track shared agent rules in .cursor/rules/)
.cursor/*
!.cursor/rules/
*.log
